export enum ImageLimit {
  MIN_COUNT = 1,
  MAX_COUNT = 20,
  MIN_ALT_TEXT_LENGTH = 5,
}

export enum LinkLimit {
  MIN_INTERNAL_COUNT = 2,
  MIN_EXTERNAL_COUNT = 1,
  MAX_TOTAL_COUNT = 50,
}

export enum ParagraphLimit {
  MIN_WORD_COUNT = 30,
  MAX_WORD_COUNT = 200,
  MIN_PARAGRAPH_COUNT = 5,
}
